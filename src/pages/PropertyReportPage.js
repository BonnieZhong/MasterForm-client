import React from 'react';

import FormComponent from '../components/FormComponent/FormComponent';

const PropertyReportPage = ({ experimentId }) => {
  return (
    <FormComponent experimentId={experimentId}/>
  );
};

export default PropertyReportPage;